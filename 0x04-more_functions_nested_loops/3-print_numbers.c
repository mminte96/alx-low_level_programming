#include "main.h"
/**
 * print_numbers - prints the numbers, from 0 to 9
 * Return: Always (Success)
 */
void print_numbers(void)
{
	int i;

	if (i >= '0' && i <= '9')
	{
		_putchar(i);
	}
	_putchar('\n');
}
