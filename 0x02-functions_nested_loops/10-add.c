#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: will be added to b
 * @b: will be added to a
 * Return: Always (Success)
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
