#include "main.h"
/**
 * _islower - write function that checks for lowercase character
 * @c: will be checked
 * Returns: 1 if c is lowercase
 * And 0 otherwise
 */
int _islower(int c)
{
	if (c >= 'a'; c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
