#include "main.h"
/**
 * print_alphabet_x10 - print 10 times the alphabet in lowercase
 * Return: alphabet
 */
void print_alphabet_x10(void)
{
	char low;
	int i;

	for (i = low; i <= 10; i++)
	{
		if (low = 'a'; low = 'z'; low++)
		{
			_putchar(low);
			_putchar('\n');
		}
	}
}
